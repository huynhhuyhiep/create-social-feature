export const parseStringToDateTime = (dateValue: string, timeValue: string = '00:00') => {
  const [year, month, day] = dateValue.split('-')
  const [hours, minutes, seconds] = timeValue.split(':');

  return ;
}
